import { Button } from '@/components/common/Button/Button';
// 공통 컴포넌트 모음 페이지
import React from 'react';

const page = () => {
    return (
        <div>
            <Button />
        </div>
    );
};

export default page;
